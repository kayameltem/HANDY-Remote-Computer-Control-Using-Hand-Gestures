class Hand:
    def __init__(self, name, fingers, distance_min, distance_max, label):
        self.name = name
        self.fingers = fingers
        self.distance_min = distance_min
        self.distance_max = distance_max
        self.label = label
